import type { SxProps } from '@mui/material/styles';

export const containerStyles: SxProps = (theme: any) => ({
    pt: theme.spacing(4),
    pb: theme.spacing(4),
    px: { xs: theme.spacing(1), sm: theme.spacing(2) },
});
---
import ListPage from '../../components/list/ListPage.astro';
import { fetchCharactersData } from '../../utils/generatedData';

const { characters } = await fetchCharactersData();
---

<ListPage mode="character" items={characters} />

---
import ListPage from '../../components/list/ListPage.astro';
import { fetchArtistsData } from '../../utils/contentData';

const { artists } = await fetchArtistsData();
---

<ListPage mode="artist" items={artists} />
